# 03 - 角色管理 (Characters) + 新建 (Create)

**绘制函数**:
- `drawCharacters` — `src/main.ts:2254`
- `drawCreatePanel` — `src/main.ts:2368`
- `drawCollectionPanel` — `src/main.ts:2503`

**触发**: 标题页 [R] / 创建后入列表 / 收集进度入口

---

## 3.1 角色管理主屏

```
┌──────────────────────────────────────────────────────────────────────┐
│                                                                       │
│                          角 色 管 理                                 │  ← 44px #c9aaff
│                                                                       │
│                                                  ┌─────────────┐     │  ← 右上 130×30
│                                                  │  收集进度   │     │     hover 18% 紫
│                                                  └─────────────┘     │
│                                                                       │
│   最近角色 (单击进入)                                                 │  ← 12px #889
│   ┌───────────┐  ┌───────────┐  ┌───────────┐                       │
│   │ 野蛮人     │  │ 法师       │  │ 圣骑士     │  ← 220×86, 单击进入  │
│   │ barb_1    │  │ mage_2    │  │ paladin_1 │   当前=金边, hover=蓝 │
│   │ Lv12      │  │ Lv8       │  │ Lv15·当前 │                       │
│   │ forest·普通│  │ desert·困难│  │ ruin·普通  │                       │
│   └───────────┘  └───────────┘  └───────────┘                       │
│                                                                       │
│   全部角色                                                            │
│    ▶ barb_1 (当前)             野蛮人 · Lv12 · 普通 · forest         │  ← 18px bold
│      mage_2                    法师 · Lv8 · 困难 · desert            │  ← 14px 副
│      paladin_1                 圣骑士 · Lv15 · 普通 · ruin           │
│      ...                                                              │  ← 仅显示前 8 行
│                                                                       │
│  [↑/↓] 选择 · [Enter] 进入/切换 · [N] 新建 · [D] 删除 · [Esc] 返回   │
│                                                                       │
│  (state.titleMsg: 创建/删除反馈, 金色 14px)                          │
└──────────────────────────────────────────────────────────────────────┘
```

**布局细节**:
- 标题 y = 64
- 最近3 卡 x: `(cx - 320) + i * 240`,y = 128,卡片 220×86
- 全部角色列表 y0 = `h/2 - rows * 26`,行高 52px
- `state.charList.slice(0, 8)` 截断 → 超过 8 角色时下拉缺失 ⚠️

**交互**:
| 键 | 动作 |
|----|------|
| `↑` / `↓` | 选中移动 |
| `Enter` | 进入/切换选中角色 |
| `N` | 打开新建面板 (`openCreatePanel`) |
| `D` | 删除二次确认 (`state.charConfirmDel`) |
| `Esc` | 返回标题 |
| 单击卡 | 进入 |

---

## 3.2 删除二次确认

```
┌──────────────────────────────────────────────────────────────────────┐
│                                                                       │
│                                                                       │
│                                                                       │
│                  删除角色 barb_1?                                    │  ← 22px #ff6a6a
│                                                                       │
│       [Y] 确认删除 (存档不可恢复) · [Esc] 取消                       │  ← 16px #f88
│                                                                       │
│                                                                       │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 3.3 新建角色面板

```
┌──────────────────────────────────────────────────────────────────────┐
│  ← 返回 (左上 200×40)                                                 │
│                                                                       │
│  ┌─────────┐  职业 [1-6]                            ┌─────────┐     │
│  │         │                                          │ 难度 [Z/X]│     │
│  │  立绘   │  ┌──────────┐                          │  默认: 普通│     │
│  │240² │  │▶1 野蛮人  │                          ├───────────┤     │
│  │         │  ├──────────┤                          │ 普通  HP×1│     │
│  └─────────┘  │ 2 圣骑士  │                          ├───────────┤     │
│  (GL层挖孔)   ├──────────┤                          │ 困难  HP×1.5│     │
│ ├──────────┤                          │ 噩梦  HP×2 │     │
│  野蛮人       │ 3 法师    │                          ├───────────┤     │
│  战士       ├──────────┤                          │ 地狱  HP×3  │     │
│  主属性:力量  │ 4 死灵    │                          │ 永久死亡  │     │
│  简介... ├──────────┤                          └───────────┘     │
│               │ 5 游侠    │                                                       │
│               ├──────────┤                            角色名 (字母数字下划线)        │
│               │ 6 刺客    │                            ┌──────────────────┐       │
│               └──────────┘                            │ barb_1▌           │  ← 48 高输入框
│                                                          └──────────────────┘       │
│   [取消]                                       [创建]                            │
│   (96×48, 红边)                              (96×48, 绿边)                      │
│                                                                       │
│  [1-6] 职业 · [Z/X] 难度 · 输入名字 · [Enter] 创建 · [Esc] 取消    │
└──────────────────────────────────────────────────────────────────────┘
```

**布局细节**:
- 职业卡: `cxp = cx - 660, cw = 220, ch = 50`,y = `118 + i * 58`
- 立绘 GL: `(cx - 320, 96, 240, 240)`
- 难度卡: `dxp = cx + 170, dw = 220, dh = 34`
- 输入框: `(cx - 260, h - 152, 400, 48)`
- 按钮: 取消 `(cx - 380, h - 152, 96, 48)`,创建 `(cx + 160, h - 152, 96, 48)`

**命名约束**: `replace(/[^a-zA-Z0-9_]/g, '_').slice(0, 24)`,默认 `barbarian_`

---

## 3.4 收集进度面板 (`drawCollectionPanel`)

触发: 角色管理右上"收集进度"按钮 / `state.collectOpen = true`

```
┌──────────────────────────────────────────────────────────────────────┐
│ ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓│ ← rgba(4,4,10,0.93)
│                                                                       │
│                          收 集 进 度                                 │  ← 30px #ffd64a
│                                                                       │
│                角色 barb_1 · 账号共享 (仓库/传承/通关)                │  ← 13px #99a
│                                                                       │
│   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                │
│   │ 角色背包+穿戴│  │ 账号仓库     │  │ 套装加成     │  ← 3 张卡       │
│   │   ...       │  │   ...       │  │   ...       │                │
│   └─────────────┘  └─────────────┘  └─────────────┘                │
│                                                                       │
│   (Esc / C / 右上关闭按钮 退出)                                       │
│                                                                       │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 文件位置

- 角色管理绘制: `src/main.ts:2254-2365`
- 新建面板: `src/main.ts:2368-2500`
- 收集面板: `src/main.ts:2503-` (后续)
- 创建确认: `src/main.ts:1895-1920`
- 创建开: `src/main.ts:1923-1930`
- 难度步进: `src/main.ts:1933-1945`
- 键盘 handler: `src/main.ts:392-450`